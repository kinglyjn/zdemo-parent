定位死锁

    检测死锁可以使用 jconsole工具，或者使用 jps 定位进程 id，再用 jstack 定位死锁:
    ```
    jps
    jstack 33200
    ```

    避免死锁要注意加锁顺序
    另外如果由于某个线程进入了死循环，导致其它线程一直等待，对于这种情况linux下可以
    1) 通过top 先定位到 CPU 占用高的 Java 进程
    2) 再利用 top -Hp <进程id> 来定位是哪个线程
    3) 最后再用 jstack 排查